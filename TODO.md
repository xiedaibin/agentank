# AgenTank XDB 坦克待优化与 Bug 跟踪列表 (TODO)

本文件用于记录坦克行为中的 Bug、待验证的战术思路以及后续的迭代规划。

---

## 📅 待讨论与实践验证的战术 (Strategic & Tactical Hypotheses)

### 1. 传送冷却真空期施压战术 (Cooling-down Vacuum Pressure Tactic)
- **背景**：在传送同门内战中，当敌方传送进入 CD 且我方传送就绪时，我方拥有绝对的位移与先手优势，可转入强力施压状态（Pressure Mode）。
- **具体实施方案（三个维度）**：
  - **维度 A：放宽暗杀（Assassination）门限**
    - *方案*：不再局限于“星数落后”或“敌方开火锁定”才执行传送背杀。只要敌方传送在 CD 且我方检测到安全的背后/侧翼破绽，直接交传送进行刺杀。
  - **维度 B：以身为饵，钓鱼避弹反杀 (Baiting)**
    - *方案*：我方主动走入敌方直道枪线（保持 6-8 格以上的安全距离），利用我方的传送做容错。一旦检测到敌方开火，立即触发闪现避弹，同时落位到对方背后实现“闪现+背杀”连招。
  - **维度 C：中场空间压缩**
    - *方案*：当敌方传送在 CD 时，缩小 `isSafe` 评估中的敌方威胁半径（如从 4 格缩减到 2 格），使我方寻路更具侵略性，强控中场与星线。
- **状态**：📝 待讨论与实践验证

---

## 🐛 已处理的 Bug (Resolved Bugs)

- **[已解决] 移动受阻开火机制 (V12.43)**
  - *问题*：对方传送落地我方前方的草丛中，由于隐身看不见，我方寻路依然发出 `go()` 动作，导致被阻挡停在原地无法位移，进入“寻路畅通 -> 移动被撞 -> 停留原地”的死循环并不发子弹。
  - *解决方案*：引入 `lastAttemptedStep`，当上一帧发出移动指令但本帧未发生位移，且目标格无障碍（判定为隐身或草丛中的敌人）时，立刻正对目标开火反击。
- **[已解决] 运行超时 Disqualified / 平局 Runtime 判负性能优化 (V12.44)**
  - *问题*：对局以平局/双方同归于尽结算时，系统会以累计运行时间（runtime）短者获胜。我方由于在热点路径（如 `buildExecutionContext` 和 `findSafeGrassSpot`）中对 `grass` 属性进行 `for..in` 遍历和 `k.split(",")` 字符串解析，导致超长对局中耗时累积超 1000ms（直接被判超时负）或在平局中因耗时长于对手被判定 runtime 负。
  - *解决方案*：将草丛数据重构为静态 `grassList` 数组，彻底移除热点循环中的字符串拆分与 `for..in` 遍历，使 128 帧的最高累积运行时间降至 280ms-580ms 左右，成功避免超时并大幅提升了 runtime 判平的胜出率。

---

## 🐛 待处理的 Bug 与基础优化 (Bugs & Refinements)

- **[待优化] 智能传送吃星判断**
  - *方案*：当对方坦克**不是传送坦克**，且根据路程计算我方坦克能比对方**更早吃到星星**时，不浪费传送吃星（保留传送 CD 留作进攻/防守容错）。
- **[待优化] 高星级守星伏击模式**
  - *方案*：当我方星星数高于对方坦克时，提高“守星压制”以及“传送靠近星并潜伏埋伏”的权重，主动开启守星模式限制对手反超。
- **当前状态**：等待下一次优化。

