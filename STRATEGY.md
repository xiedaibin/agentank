# AgenTank 战略与战术总纲 (Strategic & Tactical Masterplan)

本文件是 XDB 坦克的最高行为准则与架构设计指南。所有代码的编写、重构与自动化进化均需严格遵循本文件的设定。

## 一、 核心价值观 (Core Value Function)

胜负判定与行动优先级的严格权重，是**战术分析师**在每一帧做决策的底层逻辑：

1. **【最高】击毁对方坦克 (Destroy)**：权重 10000。如果战术推演发现在 1-3 帧内或通过 1 次传送能建立必杀（如背杀），则无条件中断抢星和巡逻。
2. **【次高】获取星星 (Stars)**：权重 500 - (距离成本)。在无击杀机会的博弈期，以最高效率（少转向、最短路径）获取星星建立胜势。
3. **【底线】运行时间与机动性 (Efficiency)**：运行时间越短、消耗动作越少越好。同等条件下，不转向的直线路径优于频繁转向的路径。

## 二、 双层分析师架构 (Dual-Analyst Architecture)

为了保证机动性和极低的计算开销，系统分为两大层面进行解耦：

### 1. 战略分析师 (Strategic Analyst - 静态层)

* **执行时机**：仅在 `game.frames === 0` 或首次遭遇敌人时执行。
* **职责**：
  * **敌方侧写 (Enemy Profiling)**：读取敌方 `skillType`，判定威胁等级（如高爆发、强控制、高机动）。
  * **战略定调**：生成 `MatchBlueprint`，设定全局常量（如对阵 Boost 时，基础安全距离 `minSafe = 8`；对阵 Control 时，危险半径 `controlRadius = 9`）。
  * **全局缓存**：分析全图障碍物与草丛，生成静态的视线缓存和寻路节点地图，避免每一帧重复计算。

### 2. 战术分析师 (Tactical Analyst - 动态层)

* **执行时机**：每一帧的 `onIdle` 循环。
* **职责**：
  * **威胁感知**：基于战略层给定的安全距离，结合当前敌方子弹和枪线，进行毫秒级避险（Dodge）。
  * **战术套用**：根据当前的距离和状态，在“通用策略”和“针对策略”中选取评分最高的一项转化为具体的 `me.go()`, `me.turn()`, `me.teleport()`, `me.fire()`。

## 三、 策略分类：通用与针对分治 (Generic vs. Specific Strategies)

### 1. 通用策略 (Generic Modules)

这是坦克生存和抢星的基础，无论面对何种技能均需运行。

* **A* 高效寻路 (A-Star Navigation)**：必须计入转向成本 (Turn Cost)，对走直线给予奖励。搜索节点上限严格控制在 600，保证执行效率。若 A* 失败，需有盲目趋近目标点的轻量级兜底移动机制。
* **基础物理避弹 (Physical Dodging)**：基于子弹速度 (2格/帧)，检测到碰撞帧数 `<= 3` 时，强制打断其他行动进行规避。
* **资源寻址 (Star Sensing)**：优先计算最近星星的路径距离，若我方星星落后，大幅提升抢星权重。

### 2. 针对性策略 (Specific Plug-ins)

此部分逻辑在“战略分析师”激活对应的 `MatchBlueprint` 后，由“战术分析师”动态挂载。

#### 1. Teleport 刺客流核心 (The Assassin Core)

* **僵直背杀 (Stun-Lock Assassination)**：监控敌方 `fireLocked` (开火僵直) 状态。在僵直期内，传送至其背后或侧翼的 5 格距离（避开极限贴脸风险），落地秒杀。
* **中场控制传送 (Center Domination)**：开局若星星距离 `> 10` 且安全，战略性交出传送抢下首星，获取视野与分数双重优势。

#### 2. 八大敌方技能针对战术 (Anti-Skill Rules)

* **对抗 Boost (加速)**：
  * *战略*：防守反击，绝对禁止在长直走廊追逐。
  * *战术*：依靠障碍物秦王绕柱，使用 **Predictive Fire (预测射击)** 封锁其 2格/帧 的必经之路。并在拉扯中优先寻找草丛。
* **对抗 Cloak (隐身)**：
  * *战略*：盲区推演 (Shadow Prediction)，将其消失点转化为不断扩大的动态危险区。
  * *战术*：启动 **Zig-Zag (Z字抖动)**，每 3 帧强制横移，破坏其隐身期间的预判狙击。
* **对抗 Control (Freeze 冰冻 / Stun 眩晕 / Poison 中毒)**：
  * *战略*：视野隔绝。将控制半径（8~9格）设为高危区，禁止空旷地带对峙。
  * *战术*：**Skill Evasion (技能规避)**：若敌方技能就绪，强制保持在控制半径外。若处于 Poison 减速状态，禁止复杂走位，强制使用直线撤退 (Straight Retreat)；若处于控制半径内，禁止非必杀的主动传送。
* **对抗 Overload (双发过载)**：
  * *战略*：轴线扩宽。敌方枪线宽度由 1 格判定为 3 格宽。
  * *战术*：普通横移无效，必须利用 `wall` 或 `mound` 进行绝对物理掩护。
* **对抗 Shield (护盾)**：
  * *战略*：骗招博弈 (Baiting)。
  * *战术*：主动卖破绽骗出护盾，然后利用速度差或障碍物避战 4 帧，护盾消失后执行反杀。
* **对抗 Teleport (同门内战)**：
  * *战略*：后手优势原则。
  * *战术*：尽量以常规走位化解攻击，保留 Teleport 作为 Counter-Teleport（反击传送）。

### 3. 超时与安全策略 (Late Game & Safety)

* **超时判优 (Late Game Aggression)**：在 900 帧后，大幅提升抢星权重。若我方星星落后或持平，采取激进的 Teleport 抢星策略。
* **瞬移安全预测 (Predictive Teleport Safety)**：在 Teleport 落地前，不仅检查当前枪线，还预测未来 3 帧的轨迹以及敌方潜在的瞬间转向攻击。落地邻近区域必须通过安全性校验。

## 四、 编码性能与复杂度约束 (Performance Standards)

1. **数据结构约束**：坚决禁止将坐标转为 `{x, y}` 对象，必须使用原生数组 `[x, y]` 以大幅降低 V8 引擎垃圾回收 (GC) 压力。
2. **空间复杂度优化**：严禁在 `onIdle` 的主循环内部反复创建大型对象或重复进行全图遍历。
3. **缓存击穿防护**：引入 `stuckCounter` 和震荡检测 `isOscillating()`，当寻路陷入死锁时，迅速降级为 `safePatrol` (盲游) 清除缓存。

## 五、 自动化稳定进化协议 (Batch Evolution Protocol)

所有的策略重构与发布，必须交由 `batch_evolution.js` 进行验证。新的评判标准为：

* **样本量**：每轮进化固定 30 场对局，减少随机概率干扰。
* **严格进退判定**：
  * `New Win Rate >= Baseline + 5%`：**显著上升**，接受代码，自动 `git commit`。
  * `Baseline - 5% < New Win Rate < Baseline + 5%`：**性能波动区**，保留代码，启动下一轮（再打 20-30 场）累积数据。
  * `New Win Rate < Baseline - 5%`：**性能退化**，立刻触发 `git restore` 还原上一版本，重新分析 `loss` 录像。
