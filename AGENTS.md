# AgenTank AI Agent 项目说明

## 项目目标

本项目的目标是为 [AgenTank](https://agentank.ai/) 平台开发一个极具竞争力的、自主运行的 JavaScript 坦克机器人。该坦克名为 **XDB**（坦克 ID 230），搭载了 `teleport`（传送）技能，旨在通过高级寻路、躲避以及基于技能冷却状态的战术拉扯，在公开排行榜上攀升。

## AI 代理初始化与学习要求（启动必做）

在第一次使用或启动本代理时，**必须**按照以下顺序执行初始化操作，方可开启任何后续工作：
1. **同步坦克规则**：首先执行“同步坦克规则”技能，将最新规则覆盖保存至本地的 [AGENT_GUIDE.md](file:///d:/MyGit/agentank/AGENT_GUIDE.md)。
2. **熟读与理解核心文档**：同步规则后，必须熟读并完全理解 [AGENT_GUIDE.md](file:///d:/MyGit/agentank/AGENT_GUIDE.md)（坦克 API 与游戏机制）和 [STRATEGY.md](file:///d:/MyGit/agentank/STRATEGY.md)（技能博弈与具体战术），在后续开发中严格遵循。
3. **开启后续工作**：在完成上述步骤后，方可开展代码优化、对战复盘、排位赛等其他开发与测试任务。

## 核心评价函数与架构约束（最高准则）

这是所有代码修改和战略制定的底层逻辑，**绝不可违背**：

1. **胜负价值观约束（评价函数）：** 最终决策收益权重必须严格遵循 **击毁对方坦克 > 获取星星数量更多 > 运行时间更短**。如果在计算中发现了一条确凿的击杀路径，其权重必须呈现压倒性优势覆盖一切抢星逻辑；反之，严禁为了吃星而陷入死角被杀。
2. **战略与战术解耦：** 代码结构必须分为“通用策略层”（基础寻路、地形计算、基础射线躲避）与“针对策略层”（针对 8 种敌方技能的特化处理，如 Anti-Boost、Anti-Cloak 插件）。两者必须解耦。详尽的战术兵法和技能对抗策略记录在 `STRATEGY.md` 文件中。
3. **强制静态缓存：** 鉴于 1v1 中敌方技能在第一帧即固定，**严禁**在每帧的寻路和躲避循环中重复推算敌方基础技能属性。必须在 `game.frames === 0` 或首次发现敌人时，构建全局静态的 `EnemyProfile`。

## 核心框架与机制

- **官方指南：** 详细的 API 参考和游戏规则请参阅本地文件 `AGENT_GUIDE.md`。
- **代码入口：** 坦克的全部逻辑均包含在 `new_tank.js` 文件的 `onIdle(me, enemy, game)` 函数中。
- **坐标系统：** 地图上的所有位置必须严格使用数组 `[x, y]` 表示，**绝不能**使用对象 `{x, y}`。
- **动作经济学：** 像 `me.turn()`、`me.go()` 和 `me.fire()` 这样的指令会将动作加入队列。转向（Turn）需要消耗 1 帧，移动（Move）需要消耗 1 帧。
- **子弹速度：** 子弹的飞行速度为 **每帧 2 格**。这意味着，如果坦克需要先转向再移动，近距离的物理躲避是绝对来不及的。
- **转向API特化（核心细节）：** 平台原生的 `me.turn(dir)` 存在非对称的指令解析规则与底层漏洞：
  - **唯一受支持参数**：文档中唯一受支持的转向参数为 `"left"` 和 `"right"`，引擎将其解析为“相对偏转 90 度”指令（左转/右转）。
  - **严禁使用 `"up"` / `"down"`**：虽然传入 `"up"` 或 `"down"` 时代码不会抛出运行时异常，但引擎**不会**将其处理为绝对方向转向。它们会触发平台的未定义随机旋转逻辑（例如，在 `down` 朝向下调用 `me.turn("up")` 会意外转到 `left`；在 `down` 朝向下连续调用 `me.turn("down")` 会发生异常的角度偏移）。
  - **架构约束**：在任何时候，都必须使用 `getTurnDir` 包装函数对目标朝向进行分流映射，将其统一转换为合法的相对转向方向 `"left"` 或 `"right"`。
  - **转向耗时与物理躲避**：180 度掉头**必须且必定消耗 2 帧**（分两步 90 度转）。在遭遇近距离子弹危险时，物理转向逃逸在时间上完全来不及，需配合其他逻辑（如传送 `teleport`），或者利用 `getTurnDir` 智能选择“不朝向敌人方向转动”的中间缓冲朝向来规避判定风险。
- **敌方技能建模：** 敌方坦克技能包含但不限于 `freeze`、`teleport`、`cloak`、`boost`、`overload`、`shield`、`poison`、`stun` 等。必须扩大危险半径并考虑隐身盲区推演，具体各技能应对详见 `STRATEGY.md`。
- **对战结果判定解释**：在分析对战录像和评估失败时，必须准确区分以下三种返回状态：
  - `crashed`：战斗中被子弹摧毁或发生碰撞，属于正常战损，非代码崩溃。
  - `runtime`：坦克代码执行超时，需要优化循环和寻路性能。
  - `error`：代码抛出运行异常，属于 Bug，需修复。

## 开发与调试规范：可视化 Speak 调试协议 (Visual Debugging Protocol)

在后续代码迭代与战术新增中，**必须**遵循以下可视化调试协议：

1. **零成本调试机制**：`me.speak("文本")` 或简写 `speak("文本")` 是平台提供的纯视觉效果。它**不消耗帧数**，**不影响坦克行为状态**，是追踪逻辑是否正确执行的最优手段。每个坦克单局比赛最多可调用 32 次，文本长度限制为 40 字符。
2. **新逻辑强制埋点**：在新增任何分支决策、避让重构或拦截逻辑时，必须在执行入口或关键跳转分支处调用 `me.speak("调试标识")`。例如：
   - 避弹逃逸：`me.speak("Ghost Evasion")`
   - 安全阀拦截：`me.speak("SO: Evasion")`（寻找安全路径）、`me.speak("SO: Stay Safe")`（原地待机安全防守）。
3. **录像复盘对齐**：在分析失败的 Replay 时，首先根据坦克头顶弹出的 Speech 文字，判断当前帧是否命中了预设的新逻辑，实现行为原因与视觉表现的精准定位。

## 用户长期授权与安全边界

- 用户已明确授权：代理可自动修改 `new_tank.js`、新增本地测试/自动化脚本、运行模拟或实战挑战、进行 Git 提交和回滚并继续迭代，无需每轮再等待审批。
- 仍需遵守安全边界：不得泄露 `.env` 中的密钥；不得使用 `{x, y}` 坐标对象；不得在没有语法验证/基础测试的情况下发布代码。
- 顶层策略者必须贯穿整个迭代过程，不能中途丢失；任何局部修补都必须接受全局胜率目标约束，防止顾此失彼。
- 当前首要目标是尽可能将实战胜率持续向 100% 推进；所有局部修改都必须以提升长期真实胜率为唯一优先级。

## 自定义自动化技能：半自动进化 (Batch Evolution)

用户可以通过指令 **“开始进化”** 或 **“开始战斗”** 来触发此技能：

1. **自动对战**：执行 `node batch_battle.js` 自动发起 10 场实战并汇总结果。
2. **深度分析**：抓取失败 Replay，分析具体败因（寻路、枪线、技能时机）。
3. **建议方案**：向用户发送《败因分析报告》及代码优化建议，等待批准。
4. **代码优化与发布**：批准后修改 `new_tank.js` 并执行 `node publish.js`。

## 自定义自动化技能：全自动复盘挑战 (Targeted Evolution Loop)

用户可以通过指令 **“全自动复盘挑战 {tankId}”** 来触发此技能。这是针对特定对手进行深度针对性优化的闭环：

1. **锚定基准**：首先记录当前代码在 `EVOLUTION_LOG.md` 中的历史基准胜率。
2. **专项挑战 (20场)**：执行 `node targeted_evolution.js <tankId>`。连续对特定对手发起 20 场挑战。
3. **主要矛盾与技能深度分析**：
   - 如果胜率 **<= 70%**，AI 代理必须分析失败录像。
   - **核心焦点**：除了基本的寻路和避弹分析，必须进行**技能博弈分析**（如：针对 Freeze 的轴线避让、针对 Teleport 的落点预判等）。
   - 提取导致失败的“主要矛盾”，提出并实施代码修改方案。
4. **达标校验与回滚**：
   - **专项达标**：专项胜率达到 **> 70%** 后，强制发起 30 场全量随机挑战。
   - **基准保障**：如果全量随机胜率低于 **70%** 或低于历史基准 **5%** 以上，判定为“过度特化导致退化”，自动执行 `git restore` 回滚代码。
5. **闭环存档**：专项达标且基准稳定后，执行 `git commit` 并更新进化日志。

## 自定义自动化技能：全自动排位无限上分模式 (Infinite Ranked Battle)

用户可以通过指令 **“打排位”** 或 **“打排位 [次数]”** 来触发此技能。

1. **执行挂机**：启动 `node ranked_battle.js [次数|infinite]` 进入对战模式。
2. **败局自动归档**：自动保存所有失败场次的录像 JSON。
3. **分析建议**：挂机结束后，建议用户通过 `analyze_replay.js` 进行重点复盘。

## 自定义自动化技能：半自动排位升星模式 (Rank Battle for Score)

用户可以通过指令 **“排位升星 [次数]”** 或 **“排位升星 [次数] best”**（例如：`排位升星 100`）来触发此技能：

1. **执行升星对战**：启动 `node batch_battleforscore.js [次数] [best|random]`。若使用 `best` 参数，脚本将自动在全服检索分值最高的合格对手进行定向挑战，以最大化提升排位积分。
2. **数据输出与报告**：对战结果与分数变化（rankScore / rankPoints）会在终端实时输出，并在结束后将详细报告保存至 `battle_report.json`。

## 自定义自动化技能：深度排位分析 (Deep Ranked Analysis)

用户可以通过指令 **“排位分析”** 来触发此技能。

1. **数据聚合**：执行 `node summarize_rank_losses.js` 提取失败录像关键特征。
2. **高阶模型诊断**：结合聚合数据、源代码及 `STRATEGY.md` 从战略、战术、算法三方面进行深度诊断，生成 Markdown 正式报告。
3. **归档与链接**：将新报告链接自动记录在 `README.md` 中。

## 自定义自动化技能：同步坦克规则 (Sync Rules)

用户可以通过指令 **“同步坦克规则”** 来触发此技能。

1. **获取最新规则**：访问 `https://agentank.ai/agent-guide`。
2. **格式化与保存**：将抓取到的内容格式化为 Markdown。
3. **覆盖本地文件**：将最新的规则保存到本地 `AGENT_GUIDE.md` 文件中，确保本地开发参考始终与官方平台保持同步。
4. **确认更新**：告知用户同步完成。

## 自定义自动化技能：挂机挑战全服第一模式 (Challenging Ranked First)

用户可以通过指令 **“挂机第一”** 来触发此技能：

1. **执行挑战**：启动 `node battle_first.js` 进入对战模式，程序将以 5 秒的频率无限轮询挑战当前符合条件的全服积分第一名。
2. **动态刷新**：每 10 场对战自动刷新一次全服排行榜，动态跟踪锁定最新分数最高的目标。

